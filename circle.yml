machine:
  services:
    - docker
  ruby:
    version: 2.3.1

dependencies:
  cache_directories:
    - ~/docker

deployment:
  hub:
    branch: master
    commands:
      - bin/rake deploy

test:
  override:
    - echo 'Skip'
